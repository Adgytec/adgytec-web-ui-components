import type { ButtonSize } from "../../core";

export const SplitButtonTriggerIconSize: Record<ButtonSize, number> = {
    "extra-small": 22,
    small: 22,
    medium: 26,
    large: 38,
    "extra-large": 50,
} as const;
